import React from "react";
import Settings from "~/app/_components/pages/Settings/Settings";

export const metadata = {
  title: "Medici Settings",
  description: "Manage your Medici account settings.",
};

const SettingsPage = () => {
  return <Settings />;
};

export default SettingsPage;
